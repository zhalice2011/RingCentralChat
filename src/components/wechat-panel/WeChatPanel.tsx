import React, { ReactElement } from 'react'

interface IProps {

}

export default function WeChatPanel (props: IProps): ReactElement {
    const {  } = props

    return (
        <div>WeChatPanel</div>
    )
}
